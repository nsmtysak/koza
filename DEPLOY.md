# 公開と更新の手順

順番は **GAS → GitHub Pages → 設定リンクを渡す**。

---

## 1. GAS プロキシ（AI機能の中継）

APIキーをアプリ側に置かないための中継役。**キーは GAS のスクリプトプロパティにしか置かない。**

### 1-1. プロジェクトを作る

1. https://script.google.com/home → 「新しいプロジェクト」
2. 名前を `Koza Proxy` などに
3. `コード.gs` の中身を**全部消して**、`gas/Code.gs`（デスクトップの `Koza-Code.gs.txt`）を**全部**貼る
4. 保存（Ctrl+S）

> エディタが赤い波線を出すことがあるが、`Utilities` などは実行時に解決されるので問題ない。
> **保存時に構文エラーが出る場合は、貼り付けが途中で切れている。** 貼り直す。

### 1-2. スクリプトプロパティ

左の歯車「プロジェクトの設定」→ 下の「スクリプト プロパティ」→ 「スクリプト プロパティを追加」

| プロパティ | 値 |
|---|---|
| `ANTHROPIC_API_KEY` | Anthropic のAPIキー |
| `TOKEN` | 合言葉。長めの英数字を自分で決める（28文字程度） |
| `MODEL_FAST` | 省略可。既定 `claude-haiku-4-5` |
| `MODEL_THINK` | 省略可。既定 `claude-sonnet-5` |

> **APIキーは絶対にスクリーンショットに写さない。** 画面に出た時点で作り直すこと。

### 1-3. デプロイ

「デプロイ」→「新しいデプロイ」→ 種類の歯車で「ウェブアプリ」

- 説明：任意
- 次のユーザーとして実行：**自分**
- アクセスできるユーザー：**全員**

> 「全員」でないとアプリから叩けない。だから合言葉を必須にしてある。

「デプロイ」→ 初回は Google の未確認アプリ警告が出る。
「詳細」→「（プロジェクト名）に移動」→「許可」。

**ウェブアプリのURL**（`https://script.google.com/macros/s/.../exec`）を控える。

### 1-4. 更新するとき（コードを直したあと）

**「新しいデプロイ」ではなく「デプロイを管理」から更新する。URLが変わらない。**

デプロイ → デプロイを管理 → 鉛筆マーク → バージョン「新バージョン」→ デプロイ

---

## 2. GitHub Pages（アプリ本体）

スマホのホーム画面に置くには HTTPS が要る（Service Worker の条件）。

### 2-1. リポジトリ

1. https://github.com/new
2. 名前：`koza` など
3. **Public**（Private だと Pages に有料プランが要る）
4. Create repository

> **合言葉もAPIキーもコードに入っていない**ので、公開して問題ない。
> `_v1/` を上げたくなければ削除してよい（旧版の参照用）。

### 2-2. 上げる

このフォルダの中身を**リポジトリのルート**に置く。

```
index.html
manifest.json
sw.js
css/
js/
icons/
gas/          ← 上げなくてもよい
```

### 2-3. Pages を有効に

Settings → Pages → Source：`Deploy from a branch` → Branch：`main` / `/ (root)` → Save

1〜2分で `https://<ユーザー名>.github.io/koza/` が開く。

---

## 3. 設定を渡す

使う本人に長いURLと28文字の合言葉を手で入れさせるのは無理がある。**設定リンク**を使う。

1. 自分のブラウザで公開URLを開く
2. 設定 → AI接続 に、GASのURLと合言葉を入れる
3. 「つながるか試す」で `つながりました（claude-haiku-4-5）` を確認
4. 「設定を渡すリンクを作る」→ リンクがコピーされる
5. **そのリンクを本人に渡す。** 開くだけで接続設定が入る

> リンクには合言葉が入っている。**リポジトリや公開の場所には置かない。**
> 手渡しなど、確実な経路でのみ共有する。

---

## 4. 動いているかの確認

公開URLをスマホで開いて、順に。

| | 見るところ |
|---|---|
| 1 | 設定 → AI接続 →「つながるか試す」→ つながりました |
| 2 | お客様 → 名刺を撮って登録 → 読み取る → 項目が埋まる |
| 3 | 右下のボタン → 文章を入れて「整理する」→ 来歴が組み上がる |
| 4 | 設定 → 目標に金額と締め日を入れる |
| 5 | 枠 → 進捗と14日の盤面が出る。「埋めるならこの方々」に締切が出る |
| 6 | 顧客カード →「お誘いの文を作る」→ 型の違う3案 |
| 7 | 顧客カード →「会う前の準備をする」→ 席で伺っておくこと、が出る |
| 8 | 今日 →「今日の段取りを組む」→ 今日やること／この先動く日 |

3・6・7・8 は GAS プロキシが要る。

---

## 5. キャッシュ（**必ず読む**）

ブラウザと Service Worker が古い JS を配ることがある。実際に2度踏んだ。

**JS か CSS を1つでも直したら、3か所を同時に上げる。**

1. `index.html` の全ての `?v=N` → `?v=N+1`
2. `sw.js` の `SHELL` 配列の `?v=N` → 同じ番号
3. `sw.js` の `VERSION`（`koza2-vN`）→ 上げる

3つがずれると、圏外での初回起動で一部だけ取得できない事故になる。

現在：**v11**（`koza2-v11`）

使う本人の端末で古いまま出るときは、アプリを一度消してホーム画面に置き直してもらう。

---

## 6. 費用

`plan` と `brief` と `invite` が Sonnet、`structure` と `card` が Haiku。

1日あたりの目安は、記録2件＋段取り1回＋準備1回＋お誘い1回で数十円程度。
名刺は送る前に端末側で長辺1400pxに縮小している（`Blobs.shrink`）。

Anthropic のコンソールで上限を設定しておくと安心。

---

## 7. お試しのデータ（お渡し前に必ず外す）

設定のいちばん下に「お試しのデータ（確認用）」がある。架空のお客様50名・来歴およそ270件を入れて、
画面の見え方を確かめるためのもの。**50名いるとどう見えるかは、3名では分からない。**

入っているもの：口座の混在（自分39／ほかの方6／ママ3／フリー2）、太客と軽めの方、
お誘い→ご来店の記録（当たりも外れも）、贈答の実績、この先2週間の予定9件、月の目標800万円。

### 外す手順

お渡しする前に、**次のどちらか**を必ずやる。

**A. データだけ消す（お試し機能は残す）**
設定 → お試しのデータ → 「全部消す」

**B. 機能ごと外す（推奨）**
1. `index.html` から `<details class="raw" id="seed-block">` のブロックを削除
2. `index.html` から `<script src="js/seed.js?v=N"></script>` の行を削除
3. `sw.js` の `SHELL` から `'./js/seed.js?v=N',` の行を削除
4. `js/seed.js` を削除
5. 5節の手順で版を1つ上げる

> **A だけで渡すと、誤って押されたときに架空のデータが本物と混ざる。**
> 混ざると見分けがつかない。本番に出すなら B にする。

---

## 8. 困ったとき

| 症状 | 原因と手当て |
|---|---|
| `合言葉が違います` | GASの `TOKEN` とアプリの合言葉が不一致 |
| `サーバー側のAPIキーが未設定です` | スクリプトプロパティの `ANTHROPIC_API_KEY` |
| `返事の形式が想定と違います` | デプロイのアクセス設定が「全員」でない |
| `不明な依頼です: plan` | GASが旧版のまま。1-4 の手順で更新 |
| `時間内に返事がありませんでした` | 電波、または GAS の実行時間上限 |
| 画面が古いまま | 5節のキャッシュ |
| 暗証番号を忘れた | 復旧手段は無い。記録ごと消すしかない（書き出しがあれば戻せる） |
